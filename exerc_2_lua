local tabela = {}

for i = 1, 100 do
    tabela[i] = math.random(1, 1000)
end

local contadorPares = 0

for i = 1, #tabela do
    if tabela[i] % 2 == 0 then
        contadorPares = contadorPares + 1
    end
end

print("Número de valores pares na tabela: " .. contadorPares)